# Provider Operations Contract Chart - 2026-05-24

## Status

Exact source contract pass completed for ambulance fleet, responder telemetry, verification/onboarding, doctor directory, and staff scheduling surfaces. Static audit only; no operational or database mutation was executed.

## Evidence Scope

- `frontend/src/components/modals/AmbulanceModal.jsx:22-82,239-260,459-665`
- `frontend/src/services/ambulancesService.js:92-224,347-366`
- `frontend/src/hooks/useAmbulances.js:158-232`
- `frontend/src/components/pages/GodModeMap.jsx:282-390`
- `frontend/src/services/emergencyResponseService.js:190-212`
- `frontend/src/services/verificationService.js:18-188`
- `frontend/src/services/orgVerificationService.js:28-245`
- `frontend/src/components/pages/VerificationQueue.jsx:118-177,711-789`
- `frontend/src/services/onboardingService.js:198-330,417-556`
- `frontend/src/components/modals/DoctorModal.jsx:24-75,168-214,315-535`
- `frontend/src/services/doctorsService.js:112-213`
- `frontend/src/components/modals/StaffSchedulingModal.jsx:122-274,485-607`
- `frontend/src/services/staffSchedulingService.js:21-160,276-377`
- `frontend/supabase/migrations/20260219000200_org_structure.sql:249-317`
- `frontend/supabase/migrations/20260219000300_logistics.sql:5-28,361-410`
- `frontend/supabase/migrations/20260219000100_identity.sql:15-44,228-271`
- `frontend/supabase/migrations/20260219000200_org_structure.sql:5-53,317,480-503`
- `frontend/supabase/migrations/20260219000700_security.sql:149-157,185-196,295-315`
- `frontend/supabase/migrations/20260219000800_emergency_logic.sql:1947-2003`
- `frontend/supabase/migrations/20260219010000_core_rpcs.sql:2076-2175`
- `frontend/supabase/migrations/20260219000900_automations.sql:86-151,646-812`

## Ambulance Fleet Contract

`AmbulanceModal` submits `formData` directly to create/update services (`AmbulanceModal.jsx:239-260`). Operational ambulance state matters to app dispatch because emergency automation responds to ambulance status, profile assignment, and current call changes.

| UI field/action | Console service payload | Database/automation receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| `call_sign`, `type`, `vehicle_number`, `hospital_id`, driver/profile assignment | Accepted by create/update payload (`ambulancesService.js:92-179`) | Columns exist on `ambulances` (`logistics.sql:5-28`) | aligned for table capability | Authorization/RLS remains part of database audit, but fields map to columns. |
| Status option `busy` | Modal offers `busy` (`AmbulanceModal.jsx:487-502`); update passes status directly (`ambulancesService.js:139-179`) | Check constraint allows `available`, `dispatched`, `on_trip`, `en_route`, `on_scene`, `returning`, `maintenance`, `offline`, `pending_approval`, not `busy` (`logistics.sql:12-16`) | confirmed drift | Selecting Busy produces a payload prohibited by current schema. |
| Fleet KPI/filter status `on_route` | `AmbulancesPage` filters and counts `status = 'on_route'` (`AmbulancesPage.jsx:96-99,142-145,185-188`). | Check constraint uses `en_route`, not `on_route` (`logistics.sql:12-16`). | confirmed read/display drift | The KPI can undercount or show zero for real en-route ambulances, while the modal writes a different invalid `busy` value. |
| `image`, `last_maintenance`, `rating` | Visible/editable in modal (`AmbulanceModal.jsx:34-36,522-655`) | Not in create payload or update whitelist and absent from `ambulances` table (`ambulancesService.js:92-179`; `logistics.sql:5-28`) | confirmed drift | UI accepts values that are not persisted. |
| Driver assignment | Modal form can submit `profile_id`/`driver_id`; service maps driver to `profile_id` (`ambulancesService.js:114-116,160-162,186-204`) | Failover trigger observes removal of `profile_id` for an active call (`automations.sql:646-812`) | drift suspected | Direct reassignment is operationally significant; active-call replacement rules need an intentional UI boundary. |
| Status mutation | Modal update writes table directly; hook also exposes direct status writer (`ambulancesService.js:139-179,347-366`) | Canonical RPC can update status/location/ETA/current call and touch hospital availability (`emergency_logic.sql:1947-2003`); failover trigger still observes direct status changes | drift suspected | Direct UI status mutation reaches failover trigger, but does not use the RPC contract that bundles dispatch telemetry. |
| Location mutation API | Service exposes direct `location` update and the exported hook calls it (`ambulancesService.js:209-224`; `useAmbulances.js:158-173,218-232`) | Request-scoped RPC updates both `emergency_requests.responder_location` and linked ambulance location after status/assignment checks (`core_rpcs.sql:2076-2175`). | confirmed parallel writer; rendered use not observed | The exported fleet hook can update an ambulance position without updating the active request telemetry that app tracking consumes. |

## Ambulance Organization And Hospital Identity Contract

Ambulance rows carry both `hospital_id` and `organization_id` (`logistics.sql:5-28`). The app ecosystem test treats the canonical chain as `ambulance -> hospital -> organization`, and separately stamps driver profile assignment when needed (`ivisit-app/supabase/tests/scripts/test_ecosystem.js:412-476,756-775`). Console must therefore keep hospital UUIDs and organization UUIDs distinct.

| Surface/action | Console behavior | Database/app receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| Page list RBAC | `AmbulancesPage` applies `applyAuthFilter(..., { orgIdField: 'organization_id' })` to count and data queries (`AmbulancesPage.jsx:77-114`). | `ambulances.organization_id` exists and references `organizations.id` (`logistics.sql:5-28`). | aligned direction | This page-level list path scopes by organization UUID correctly when the row has `organization_id` populated. |
| Service list RBAC | `getAmbulances()` applies `applyAuthFilter(..., { orgIdField: 'hospital_id' })`, which resolves org-admin hospital IDs through `getCurrentUser()` (`ambulancesService.js:17-39`; `authService.js:17-39,92-130`). | Hospital-scoped filtering is valid only when `user.hospital_ids` has been resolved from `profiles.organization_id -> hospitals.organization_id`. | parallel read contract | The page and service use different scope fields for the same table. Implementation should choose one canonical scoping route or deliberately keep both with proof. |
| Org-admin create default | `AmbulanceModal` sets `formData.hospital_id = orgId` for org-admin create (`AmbulanceModal.jsx:72-73`). | `orgId` from auth is `profiles.organization_id`, an `organizations.id`, while `ambulances.hospital_id` must reference `hospitals.id`. | confirmed identity drift | Org-admin ambulance creation can submit an organization UUID as a hospital UUID. This can fail FK validation or create a row with missing/incorrect station truth if constraints are bypassed elsewhere. |
| Driver select auto-populates station | Selecting a driver sets `hospital_id: profile.organization_id` (`AmbulanceModal.jsx:182-191`). | Driver profiles store organization UUIDs; app setup separately attaches ambulance `hospital_id` to a hospital and profile `assigned_ambulance_id` to the driver (`test_ecosystem.js:451-476`). | confirmed identity drift | Choosing a driver can overwrite a valid hospital station with the driver's organization UUID. |
| Org-admin scoped stats | `AmbulancesPage` org-admin stats query filters `ambulances.hospital_id = orgId` (`AmbulancesPage.jsx:173-197`). | `orgId` is organization UUID, not hospital UUID. | confirmed stats drift | Org-admin stats can show zero or incomplete fleet counts even when the list query returns organization-scoped rows. |
| Station display | Grid renders `STATION: {ambulance.hospital || 'HQ'}` (`AmbulancesPage.jsx:612-617`), while rows are fetched as `ambulances.select('*')` without a hospital join. | Table stores `hospital_id`, not `hospital` text; legacy name fallback exists only in modal lookup (`AmbulanceModal.jsx:195-209`). | confirmed display drift | Cards often show `HQ` even when a valid `hospital_id` exists. Station display needs a hospital join or a normalized projection. |
| Organization column on create/update | Service accepts both `hospital_id` and `organization_id` from form input (`ambulancesService.js:92-179`). Modal never deliberately sets `organization_id` from the selected hospital. | App tests verify the hospital-to-organization chain; SQL table also has a direct `organization_id` FK. | drift suspected | A new ambulance can carry hospital truth without matching organization truth, or vice versa. Implementation should derive `organization_id` from the selected hospital or use a guarded receiver. |

## Ambulance Responsive Projection Contract

Desktop grid, list, table, and mobile fleet surfaces all receive raw ambulance rows from `ambulances.select('*')` (`AmbulancesPage.jsx:104-158`). None of these projections joins `hospitals`, so station labels and some metrics depend on legacy/display-only fields that the current ambulance table does not own.

| Surface field | Current render assumption | Database/app receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| Station label | Grid, table, list, and mobile surfaces render `ambulance.hospital || 'HQ'` (`AmbulancesPage.jsx:612-617`; `AmbulanceTableView.jsx:117`; `AmbulanceListView.jsx:34`; `MobileAmbulances.jsx:373,389`). | `ambulances` stores `hospital_id`; hospital name lives on `hospitals.name`. | confirmed display drift | Station display can show `HQ` for a correctly linked ambulance because the page never joins or maps the hospital row. |
| Rating | Grid/table/list/mobile render `ambulance.rating` (`AmbulancesPage.jsx:590-596`; `AmbulanceTableView.jsx:116`; `AmbulanceListView.jsx:41`; `MobileAmbulances.jsx:173-176,404`). | `ambulances.rating` is absent from the table source and earlier live check found it absent from the exposed surface. | confirmed display-only metric | The fleet can show `N/A` or average `0.0` from a UI-only field. Do not treat rating as operational quality until a receiver exists. |
| Busy KPI | Desktop and mobile both keep `busy` as a KPI/filter (`AmbulancesPage.jsx:97-99,143-145,805-827`; `MobileAmbulances.jsx:97-137,180-188`). | Ambulance status constraint does not allow `busy`; active workload should come from `on_trip`, `dispatched`, `en_route`, `on_scene`, or request assignments. | confirmed display drift | Busy units can remain zero or filter to impossible rows even when fleet is active. |
| En Route KPI | Desktop filters exact `status = 'on_route'`; mobile accepts `on_route` or `en_route` (`AmbulancesPage.jsx:97,143`; `MobileAmbulances.jsx:97,155-160,180-188`). | Table allows `en_route`, not `on_route`. | confirmed responsive mismatch | Desktop and mobile can disagree on the same fleet population. |
| Vehicle identifier | Surfaces render `vehicle_number`; dispatch RPC falls back to `license_plate` then `vehicle_number` for responder vehicle plate (`core_rpcs.sql:2740-2798`). | Both columns exist on `ambulances`. | partial alignment | Normalize operator vehicle label to `license_plate || vehicle_number || call_sign` so fleet cards and responder details follow the same available identifier order. |
| Non-ASCII separators/arrows | Fleet source files intentionally use bullet and arrow characters in visible copy/comments (`AmbulancesPage.jsx:465,578`; `AmbulancesService.js:105`; `authService.js:123-138`). | Text-only source hygiene. | encoding-gate required | These are not mojibake in the current scan, but future edits to fleet files must run the encoding/mojibake check and keep separators intentional. |

## Responder Telemetry Contract

| Surface/action | Console receiver | SQL/app truth boundary | Status | Finding |
| --- | --- | --- | --- | --- |
| Driver publishes browser location from live map | `GodModeMap` finds an active ambulance request and calls `updateResponderLocation(request.id, {lat, lng}, heading)` (`GodModeMap.jsx:282-390`; `emergencyResponseService.js:190-212`). | `console_update_responder_location` authorizes assigned responders/org operators, permits only active statuses, validates geometry, and updates both request and linked ambulance (`core_rpcs.sql:2076-2175`). | aligned canonical path | The rendered operations-map telemetry control uses the request-owned mutation needed for patient realtime recovery. |
| Generic fleet location update | `useAmbulances.updateLocation()` remains public and calls direct table update (`useAmbulances.js:158-173,218-232`; `ambulancesService.js:209-224`). | Patient tracking reads request responder truth; request-scoped RPC is the coupled writer. | confirmed ownership hazard | Keep fleet CRUD location editing out of active-dispatch tracking, or replace it with the request-scoped receiver where an active request exists. |

## Driver Assignment And Trip Status Contract

The driver-management service is rendered from `GodModeMap` and `AmbulanceModal`. It mostly uses RPCs for trip status changes, but its read model and utilization math still carry status and identity drift.

| Surface/action | Console behavior | Receiver/app contract | Status | Finding |
| --- | --- | --- | --- | --- |
| Active assignment list | Reads `emergency_requests` with `service_type = 'ambulance'` and statuses `in_progress`, `accepted`, `arrived`, then manually joins hospitals, patient profiles, and ambulances (`driverManagementService.js:17-76`). | App request and tracking views read responder/trip fields from the emergency request and linked ambulance. | aligned direction | The active assignment read model is request-owned, which matches patient tracking better than deriving trips from ambulance rows alone. |
| Driver utilization | Counts total drivers as ambulance rows, busy drivers as `ambulances.status === 'busy'`, and on-trip drivers as active request count (`driverManagementService.js:84-121`). | Ambulance status constraint does not allow `busy`; valid operational statuses include `dispatched`, `on_trip`, `en_route`, and `on_scene`. | confirmed display-truth drift | Busy-driver metrics can never be truthful under the current enum, and utilization mixes vehicle count with active request count rather than driver profile availability. |
| Arrived/completed/cancelled actions | Modal and map call `updateTripStatus()`, `completeTrip()`, or `cancelTrip()`, which use console RPCs for status changes (`AmbulanceModal.jsx:756-776`; `GodModeMap.jsx:389`; `driverManagementService.js:130-225`). | `console_complete_emergency`, `console_cancel_emergency`, and `console_update_emergency_request` are guarded RPC receivers. | mostly aligned status receiver | The mutation path is better than direct table writes, but success copy such as "Driver marked as available" should be verified against actual returned ambulance/request state. |
| Driver pool | `getDrivers()` and `getAvailableDrivers()` read `profiles.provider_type = 'driver'`, and available drivers use `assigned_ambulance_id IS NULL` (`ambulancesService.js:251-283`). Ambulance assignment writes `ambulances.profile_id`, not `profiles.assigned_ambulance_id` (`ambulancesService.js:186-204`). | App demo staff setup syncs driver profile role and also stamps assigned ambulance on the profile after creating the ambulance. | confirmed identity drift | A driver assigned by console through `ambulances.profile_id` can still appear available if `profiles.assigned_ambulance_id` is not updated by a trigger or RPC. |

## Provider Verification And Onboarding Contract

Hospital verification is the patient-facing dispatch authority. Updating a hospital's `verified` or `verification_status` is projected into `dispatch_eligible` by `trg_sync_dispatch_eligibility`, together with status and emergency eligibility (`org_structure.sql:480-503`).

| Flow/action | Console behavior | Database/app receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| Facility approve/reject in active verification queue | `verifyOrganization()` updates `hospitals.verification_status` and `verified` (`orgVerificationService.js:126-175`; `VerificationQueue.jsx:145-156`). | Hospital verification is an input to `dispatch_eligible` trigger projection (`org_structure.sql:480-503`). | aligned direction for admin approval | This is the Console lane that governs whether hospital truth can become dispatchable, subject to `emergency_eligible` and availability. |
| Person/provider approve/reject | `verifyProvider()` labels action provider verification but directly updates `profiles.bvn_verified` (`verificationService.js:128-188`). | Profiles are updatable only by their owner under current RLS (`security.sql:149-157`); dispatch eligibility is owned by hospital verification, not profile BVN (`org_structure.sql:480-503`). | confirmed receiver and meaning drift | Admin approval of another provider is blocked by the source policy, and even a permitted BVN write would not certify a facility for patient dispatch. |
| New organization submission | Onboarding inserts a `hospitals` row, then stores that returned hospital ID into `profiles.organization_id` (`onboardingService.js:198-264`). | `profiles.organization_id` references `organizations.id`, while current hospital policy source provides SELECT but no direct onboarding INSERT policy (`org_structure.sql:5-53,317`; `security.sql:185-196`). | confirmed identity and authorization drift | The onboarding flow treats a hospital as an organization and has no evidenced RLS-authorized facility creation boundary. |
| Legacy onboarding approval helpers | `approveOrganization()` / `rejectOrganization()` write `verified_at`, `rejected_at`, `rejection_reason`, and `profiles.verification_status` (`onboardingService.js:484-551`). No current component invocation was found. | These fields are absent from the declared hospital/profile table shapes (`org_structure.sql:19-53`; `identity.sql:15-44`). | exposed stale API | The active queue uses `orgVerificationService`; legacy onboarding approval methods should not be adopted as an implementation source without removal or remapping. |

## Verification Queue Field-To-UI Contract

The verification page renders two trust lanes through one page: provider/person review from `profiles` and facility review from `hospitals` (`VerificationQueue.jsx:97-156,333-334`). The shared UI vocabulary hides important receiver differences.

| UI/status field | Current service/data source | Receiver expectation | Status | Finding |
| --- | --- | --- | --- | --- |
| Provider `status = pending` | `getVerificationQueue()` filters `profiles.role = provider` and `bvn_verified = false` (`verificationService.js:51-52`). | `profiles` has `bvn_verified`, not `verification_status` (`identity.sql:15-44`). | aligned for pending BVN queue | Pending provider queue is a BVN/profile review queue, not a facility-verification queue. |
| Provider `status = approved` | Filters `bvn_verified = true` (`verificationService.js:53-54`). | `bvn_verified = true` can also auto-complete onboarding for patient roles under `recalculate_onboarding_status` (`identity.sql:228-271`). | partial alignment | The field can represent person verification, but it should not be labelled as provider dispatch certification. |
| Provider `status = rejected` | Filters the same as pending: `role = provider` and `bvn_verified = false` (`verificationService.js:55-56`). Stats do not compute rejected providers (`verificationService.js:94-103`). | No rejected-provider field or notes receiver exists on `profiles`. | confirmed status collapse | Reject and pending are indistinguishable in storage. Rejected provider cards can reappear as pending and cannot preserve a rejection reason. |
| Provider row badge | Table/list views render `provider.verification_status || 'pending'` (`VerificationQueueTableView.jsx:127-128`; `VerificationQueueListView.jsx:103-104`). | Provider queue selects from `profiles`, where `verification_status` is absent (`identity.sql:15-44`). | confirmed display drift | Provider rows always fall back to pending unless a non-schema field is injected elsewhere. `bvn_verified` is the actual field currently available. |
| Organization `status = approved` filter | Shared filter schema and KPI cards set `filters.status = 'approved'` (`VerificationQueue.jsx:297-299,450-452`; `MobileVerification.jsx:45-47`). | `getOrgVerificationQueue()` expects `verification_status = 'verified'`, not `approved` (`orgVerificationService.js:43-58`; `org_structure.sql:41`). | confirmed filter drift | Clicking the approved/verified organization KPI can query `verification_status = approved` and return an empty or misleading organization queue. |
| Organization row status | Organization cards render `org.verification_status` and call `verifyOrganization(org.id, approved)` (`VerificationQueue.jsx:761-789`; `MobileVerification.jsx:330-337`). | `verifyOrganization()` writes `verified` or `rejected` plus boolean `verified` (`orgVerificationService.js:149-155`). | aligned mutation receiver | Facility approval is the correctly shaped dispatch-trust mutation path, but the UI filter vocabulary must map `approved` UI intent to `verified` storage truth. |
| Mobile provider pending check | Mobile treats providers as pending when `!item.bvn_verified` and uses optional `item.verification_status` as a secondary condition (`MobileVerification.jsx:62,89-97,281-283`). | `profiles.verification_status` is absent. | confirmed mixed-field assumption | Mobile can render the same provider differently if a stale injected `verification_status` appears. It should derive provider state from one explicit projection. |
| Bulk approve/reject | Bulk action buttons only show success toast and clear selection (`VerificationQueue.jsx:840-853`). | No bulk service call or receiver transaction is executed. | confirmed no-op action | The page promises a privileged operation without mutating provider or organization truth. Disable or implement through guarded receivers before relying on it operationally. |
| Verification notes | `VerificationModal` collects `verification_notes` and has edit-mode save plumbing (`VerificationModal.jsx:36-50,242-271`). | No `profiles.verification_notes` column exists in current identity table source; view mode approval only calls `verifyProvider(id, boolean)`. | display/edit drift | Notes are not currently persisted through the active approval path. |

## Onboarding And Facility Claim Contract

Onboarding is the path that should create or claim provider supply for the app. Today it blends organization, hospital, and profile identity in ways that can block the flow or leave orphaned pending facilities.

| Flow/action | Console behavior | Database/app receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| Duplicate service method | `onboardingService` declares `searchHospitalsByName` twice; the later definition overrides the earlier one in the exported object (`onboardingService.js:138-184,370-408`). | Current `hospitals` source includes `verification_status`, but the later method comments claim only `verified` exists (`org_structure.sql:19-53`). | confirmed stale implementation branch | The active search path discards pending-vs-unclaimed distinction even though the schema has `verification_status`. |
| Claim status lookup | `checkHospitalClaimStatus()` selects only `verified` and `status`, then treats `verified = false` as claimable (`onboardingService.js:417-456`). | Facility lifecycle has both `verified` and `verification_status`; pending rows can be `verified = false`. | confirmed claim-state drift | A pending facility can be presented as available to claim because the check ignores `verification_status = pending`. |
| New submission insert | `submitOnboarding()` inserts a new row into `hospitals` with verification pending (`onboardingService.js:219-243`). | Hospital insert policy for unaudited onboarding was not proven in the current RLS source. | needs authorized receiver | If RLS blocks the insert, the user sees onboarding failure. If it succeeds through broad policy, the receiver still lacks explicit organization creation/linkage. |
| Profile organization link | After hospital insert, profile update sets `organization_id: organization.id`, where `organization.id` is the new hospital UUID (`onboardingService.js:252-264`). | `profiles.organization_id` is constrained to `organizations.id` (`org_structure.sql:317`; app reference marks `hospitals.organization_id` as the hospital-to-organization link in `supabase/docs/REFERENCE.md:47`). | confirmed FK identity drift | The profile update can fail or store the wrong identity if constraints differ live. Either way, org-admin scope and wallet/pricing/dispatch ownership are not established correctly. |
| Organization display ID | Success page receives `organization: { ...organization, display_id }` from the inserted hospital row (`onboardingService.js:282-298`). | Hospital display IDs use `HSP-`; organizations use `ORG-` (`ivisit-app/supabase/docs/REFERENCE.md:47-53`). | confirmed naming drift | The UI can call a hospital row an organization and surface a hospital display ID as the organization registration identifier. |
| Approval side effects | Active queue facility approval writes only hospital verification fields (`orgVerificationService.js:149-155`). | Organization wallet creation is triggered by new `organizations` rows, not hospital rows (`automations.sql:69-82`). | missing ecosystem side effect | A verified hospital created by onboarding does not automatically prove an organization row, wallet row, Stripe ownership, or org-admin authority exists. |

## Organization Registry Contract

The organization registry is console-only operational administration, but its rows are cross-cutting app infrastructure. Hospitals, wallets, payments, profiles, pricing scope, and dispatch authorization all depend on the same organization UUID.

| UI field/action | Console behavior | Database receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| Organization create/edit | Admin route exposes name, contact email, revenue share, active flag, and Stripe account id (`OrganizationsPage.jsx:179,280-327,538-585`). Service writes direct `organizations` insert/update (`organizationsService.js:46-68,139-153`). | `organizations` declares only these base fields plus display id/timestamps (`org_structure.sql:5-15`). | field-aligned for base row | The visible edit fields match the base table shape, but direct writes depend on admin policy that is not present in current RLS source. |
| Organization read/list | Service reads all `organizations` plus all `organization_wallets`, then maps wallet balance in JS (`organizationsService.js:82-123`). | Current RLS source grants only public SELECT for active organizations (`security.sql:186-188`) and org-admin own wallet SELECT (`security.sql:239-241`). | confirmed authorization drift | The admin registry promises full active/inactive organization management, but source policy only proves public active org reads. |
| Organization delete | Page confirms destructive delete (`OrganizationsPage.jsx:96-111`); service calls direct table delete (`organizationsService.js:160-171`). | No current source policy grants organization DELETE; `handle_new_organization` only creates a wallet after insert (`automations.sql:69-82`). | confirmed destructive-authority gap | Delete should not remain a direct browser table action without a guarded admin RPC and dependency checks for hospitals, wallets, profiles, payments, and pricing. |
| Registry metrics | Page renders static network health `99.8%` and aggregates wallet/fee values (`OrganizationsPage.jsx:402-404`; `OrganizationTableView.jsx:53-115`; `OrganizationListView.jsx:53-72`). | Wallet balance exists on `organization_wallets`; no network health receiver was found. | display-truth drift | Wallet numbers are receiver-backed when visible, but network health is a presentation constant and should be labelled, removed, or backed by a real metric owner. |
| Stripe connection label | Views label `stripe_account_id` as connected/live (`OrganizationTableView.jsx:126-136`; `OrganizationListView.jsx:72`). | Organization table stores `stripe_account_id`; card/customer/payout receiver drift is documented in the finance chart. | partial alignment | Connected-account presence is a valid field, but it must not imply cards, customer, payout method, or wallet readiness. |

## Hospital Facility And Discovery Contract

The hospital table is both console-managed supply and app discovery truth. The app-owned nearby-provider RPCs now depend on taxonomy fields, eligibility flags, provider source, verification state, and coordinates (`ivisit-app/supabase/migrations/20260219010000_core_rpcs.sql:11-123`). Console create/update still exposes a narrower facility form.

| UI field/action | Console service payload | Database/app receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| Facility type/tier | Modal edits `type` as `premium`, `standard`, or `basic` (`HospitalModal.jsx:481-493`); service persists `type` (`hospitalsService.js:110-111`). | App discovery taxonomy uses `provider_type`, not `type`; `nearby_hospitals` filters `provider_type = 'hospital'` and emergency eligibility (`ivisit-app/core_rpcs.sql:42-49`). | confirmed semantic drift | Console tier is a visual/business tier, not provider category. It cannot intentionally manage pharmacies, labs, clinics, or Explore Care taxonomy. |
| Emergency/booking taxonomy | No modal fields or payload whitelist for `provider_type`, `emergency_eligible`, `booking_eligible`, `provider_source`, or `category_confidence` (`HospitalModal.jsx:25-45`; `hospitalsService.js:15-34,42-61`). | Columns exist and drive `nearby_hospitals`/`nearby_providers` (`org_structure.sql:415-421`; `core_rpcs.sql:26-31,78-84`). | missing console capability | Console can create/edit a row but cannot deliberately control the app-discovery category, emergency eligibility, booking eligibility, or provenance contract. |
| Dispatch eligibility | Modal toggles `verified` and stores `verification_status` through facility approval paths (`HospitalModal.jsx:817-826`; `orgVerificationService.js:149-155`). | `trg_sync_dispatch_eligibility` computes `dispatch_eligible` from `emergency_eligible`, `status`, `verified`, `verification_status`, and demo/partner markers (`org_structure.sql:479-503`). | partial alignment | Verification can release dispatch eligibility only when `status = available` and `emergency_eligible = true`. Console should render derived `dispatch_eligible` instead of implying the checkbox alone is sufficient. |
| Operational status | Modal offers `available`, `busy`, and `closed` (`HospitalModal.jsx:793-805`); service forwards `status` (`hospitalsService.js:157-158`). | App discovery filters `status = 'available'`; trigger sets `booking_eligible = (status = 'available')` (`core_rpcs.sql:48,122`; `org_structure.sql:492-493`). | aligned by exclusion, but vocabulary needs proof | `busy` is not a discovery-visible state. If used, it removes the facility from booking/emergency results, but it is not a documented patient-facing reason. |
| ER wait minutes | Modal collects `emergency_wait_time_minutes` (`HospitalModal.jsx:629-631`); service includes it in update payload (`hospitalsService.js:135-137`). | `update_hospital_by_admin` does not read `emergency_wait_time_minutes`; it updates `wait_time` but not the numeric field (`core_rpcs.sql:242-279`). | confirmed update drift | Editing ER Wait can show a save success while the numeric app/console field remains unchanged on update. Create can insert it; update cannot. |
| Bed scalar update | Modal edits scalar `available_beds`, `icu_beds_available`, and `total_beds` (`HospitalModal.jsx:571-604`); update RPC writes scalar fields and touches `last_availability_update` (`core_rpcs.sql:266-281`). | Bed normalization trigger maintains `bed_availability` from scalar changes (`org_structure.sql:180-245`). | aligned receiver with previous correction | Scalar capacity edits do reach the update receiver and trigger path; room-bucket/pricing completeness remains a separate bed-management issue. |
| Array fields | Modal edits comma-separated `specialties` and `features`; service also supports `service_types` (`HospitalModal.jsx:527-544`; `hospitalsService.js:116-118`). | Update RPC always assigns `specialties`, `service_types`, and `features` from payload-derived arrays (`core_rpcs.sql:233-238,282-286`). | partial update hazard | Modal full saves send arrays, but any partial caller of `updateHospital()` that omits these keys can clear arrays because the RPC initializes absent array inputs to empty arrays. |
| Google autofill search | Modal fetches `/functions/v1/discover-hospitals` with `{ query, mode: 'text_search', limit: 5 }` and reads `data.hospitals` (`HospitalModal.jsx:146-175`). | App-owned handler requires latitude/longitude before provider discovery and returns `{ data: limitedResults, meta: ... }`, not a `hospitals` key (`ivisit-app/discover-hospitals/handler.ts:120-303`). | confirmed response-shape drift | The create modal can show no Google results even when the function returns data, and text search without coordinates does not match the app handler contract. |
| Google result mapping | Selection reads `hospital.geometry.location.lat/lng` and `hospital.types` (`HospitalModal.jsx:274-285`). | App normalized provider rows expose direct `latitude`, `longitude`, `provider_source`, category, and media fields after normalization. | confirmed shape drift | Even if search results load, the selector assumes raw Google Places shape rather than normalized app provider rows. |
| Display ID label | Hospital table falls back to `ORG-PENDING` for hospital rows (`HospitalTableView.jsx:143`). | Hospital display prefix is `HSP-`; organizations use `ORG-` (`ivisit-app/supabase/docs/REFERENCE.md:47-53`). | confirmed label drift | Facility rows can display an organization-style pending ID even though the row is a hospital. |
| Media provenance | Modal uploads/stores `image` only (`HospitalModal.jsx:835-901`; `hospitalsService.js:112-114`). | App-side hospital media fields include `image_source`, `image_confidence`, `image_attribution_text`, and `image_synced_at` (`ivisit-app/org_structure.sql:321-326`). | provenance drift | Manual image changes can overwrite visible media without recording whether it came from console upload, discovery, demo seed, or provider sync. |

## Doctor Directory And Invite Contract

`DoctorModal` supports linking an existing provider profile or using manual entry/invite. Manual create starts with `profile_id: ''` (`DoctorModal.jsx:30-42,315-351`) and creates the doctor row before attempting invitation (`:168-208`).

| Flow/field | Console behavior | Trigger/schema behavior | Status | Finding |
| --- | --- | --- | --- | --- |
| Doctor directory fields | `createDoctor()` inserts profile, facility, name, specialty, status, license, email and phone (`doctorsService.js:112-146`) | Columns exist in `doctors` (`org_structure.sql:249-273`) | aligned for raw directory row | Basic field mapping is present. |
| Manual create with invite enabled | Inserts a doctor row first; then invokes provider invitation with `provider_type: 'doctor'` (`DoctorModal.jsx:168-208`) | On a new doctor profile insert/update, `sync_doctor_record_from_profile` upserts a doctor row keyed by `profile_id` (`automations.sql:86-151`) | confirmed drift | A manual doctor row without a profile link can coexist with the new trigger-created profile-linked row after invitation. |
| Linked existing profile | UI states that link bypasses invitation (`DoctorModal.jsx:327-351`) and create includes `profile_id` | Trigger uses `ON CONFLICT (profile_id) DO UPDATE` (`automations.sql:114-140`) | aligned direction; runtime proof pending | Linking first matches trigger ownership better than create-then-invite. |
| Direct doctor update/delete | Service writes or deletes `doctors` directly (`doctorsService.js:151-191`) | Profile trigger can later overwrite name/email/phone/hospital fields for linked doctors (`automations.sql:134-151`) | drift suspected | Edit ownership is split between directory CRUD and profile projection. |

## Staff Scheduling Contract

The database defines `doctor_schedules` with actual `doctor_id`, `date`, `start_time`, `end_time`, `shift_type`, and `is_available` rows (`ivisit-app/supabase/migrations/20260219000200_org_structure.sql:275-285`). Source RLS grants public reads and org-admin/admin management for doctor rows belonging to the operator's organization (`ivisit-app/supabase/migrations/20260219000700_security.sql:441-455`). The modal collects shift fields (`StaffSchedulingModal.jsx:122-274,485-607`), but the service synthesizes schedules from status and never reads or writes the authorized receiver.

| UI field/action | Service receiver | Table contract | Status | Finding |
| --- | --- | --- | --- | --- |
| `date`, `start_time`, `end_time`, `shift_type`, `notes` on add | Form submits these fields (`StaffSchedulingModal.jsx:122-185,485-607`) | `createStaffSchedule()` writes only `doctors.status` and `updated_at` (`staffSchedulingService.js:276-312`); `doctor_schedules` owns shift fields but does not own `notes` | confirmed drift | Successful "scheduled" feedback does not persist the shift date, time, or type; the unsupported notes control must be removed or assigned a deliberate schema receiver. |
| Displayed doctor shifts | Service derives today's fixed `09:00` to `17:00` schedule from every doctor row (`staffSchedulingService.js:79-128`) | `doctor_schedules` rows are not read | confirmed drift | UI renders a projection, not stored scheduling data. |
| Edit/delete shift | Service parses synthetic `doctor_{id}` and toggles doctor status (`staffSchedulingService.js:317-377`) | Does not update or delete `doctor_schedules` | confirmed drift | A user cannot manage actual shifts through this scheduling surface. |
| Conflict detection | `checkScheduleConflicts()` accepts date/time parameters but checks only current `doctors.status` (`staffSchedulingService.js:466-492`) | Real overlap checks must query `doctor_schedules` by doctor/date/time window | confirmed drift | A current busy state is not proof of a shift collision, and an overlapping stored shift is not detected. |
| Shift stats | `getScheduleStats()` counts doctor/fleet statuses as scheduled shifts and filters ambulances through absent `ambulances.hospital` (`staffSchedulingService.js:383-461`) | Real schedule totals must aggregate `doctor_schedules`; `ambulances` owns `hospital_id` and `organization_id`, not `hospital` | confirmed drift | Summary figures are status projections and the ambulance branch can fail at the schema boundary. |
| Ambulance crew schedule | Service derives shifts from `ambulances.crew`, selects absent `ambulances.hospital`, then explicitly refuses crew schedule creation (`staffSchedulingService.js:32-77,299-302`) | No persisted crew scheduling receiver shown; current logistics schema owns `hospital_id` and `organization_id` only | confirmed limitation | UI must not imply ambulance shift CRUD until a data owner exists, and must not render fabricated full-day crew schedules. |

Determined implementation contract: Pass 5 adopts `doctor_schedules` as the sole doctor-shift CRUD/read/conflict/statistics owner through its existing authorized organization boundary. Doctor operational availability remains a distinct `doctors` state concern. Ambulance assignment and fleet availability remain visible operational context only; ambulance shift CRUD and generated date/time rows are excluded until a persisted crew-schedule receiver is deliberately introduced.

## Provider Media And Storage Contract

Provider media is a cross-pass storage boundary. `storageService.uploadImage()` writes to the public `images` bucket and returns a public URL (`storageService.js:3-28`). The active modal consumers are hospital, doctor, and ambulance forms (`HospitalModal.jsx:132`; `DoctorModal.jsx:74`; `AmbulanceModal.jsx:217`). No file type, file size, image dimension, or per-folder authorization guard is enforced in this helper; it relies on deployed Storage policies.

Current source policy evidence is incomplete: active console/app migrations define table RLS for public tables, but the storage object policy matches found in this pass are archive references. Implementation must verify the deployed `storage.objects` policies for `images` and `documents` before treating upload success or failure as a UI-only issue.

| Surface/action | Console behavior | App/database receiver | Status | Finding |
| --- | --- | --- | --- | --- |
| Hospital image upload | `HospitalModal` uploads to `images/hospitals/*`, stores the returned URL in `formData.image`, and `hospitalsService` forwards `image` through create/update payloads (`HospitalModal.jsx:132`; `hospitalsService.js:17-44,113-114,267-288`). | App hospital media also tracks `image_source`, `image_confidence`, `image_attribution_text`, and `image_synced_at` (`ivisit-app/supabase/migrations/20260219000200_org_structure.sql:323-326`; app provider media helpers read/write those fields). | partial alignment with provenance drift | The URL can persist, but console does not set media provenance. Manual uploads may overwrite or coexist with app discovery/media pipeline assumptions without source/confidence/attribution truth. |
| Doctor image upload | `DoctorModal` uploads to `images/doctors/*`, stores the URL in `formData.image`, and `doctorsService` persists `image` on create/update (`DoctorModal.jsx:74`; `doctorsService.js:112-164`). | `doctors.image` exists in the org-structure table and is app-visible provider presentation data. | aligned for raw image URL | Keep image on the doctor directory row under the current receiver; profile-projected name/email/phone/facility fields remain separate and must not be silently overwritten by image editing. |
| Ambulance image upload | `AmbulanceModal` uploads to `images/ambulances/*` and previews the URL (`AmbulanceModal.jsx:217`), but `ambulancesService` create/update payloads omit `image` and the live table surface lacks `ambulances.image`. | `ambulances` table owns dispatch/fleet status, not media in the current source/live shape. | confirmed drift | The UI can upload an object that is never linked to a persisted ambulance row, creating orphaned media and false save confidence. |
| Upload helper path authority | `storageService.uploadImage(file, folder)` accepts any caller-supplied folder and returns a public URL. | Archive storage policy references show both per-user folder variants and broad authenticated `images` writes; current active source did not prove which is deployed. | needs read-only deployment proof | Before implementation, verify bucket public/private flags and `storage.objects` policies. Do not infer authorization from the client helper. |

## Implementation Planning Boundary

| Area | Minimum forward contract before code changes |
| --- | --- |
| Ambulance form | Remove or map unsupported fields and replace invalid status choices; use request-scoped RPC controls for active-dispatch status/location and keep non-active fleet administration explicitly separate. |
| Ambulance identity | Never write an organization UUID into `hospital_id`. Resolve org-admin station choices to hospital UUIDs, derive `organization_id` from the hospital when needed, and make page/service scoping consistent. |
| Ambulance responsive projection | Join or normalize hospital station labels, remove display-only `rating` until backed, and make desktop/mobile status KPIs use the same valid enum set. |
| Active responder telemetry | Retain request-scoped telemetry as canonical; retire or guard direct fleet location mutation for active calls. |
| Verification ownership | Use hospital verification for dispatch trust; define separately whether person credential/BVN review belongs in Console and provide an authorized receiver if it does. |
| Verification queue projection | Split provider BVN states from facility verification states, map organization UI `approved` to stored `verified`, remove no-op bulk actions, and do not render provider `verification_status` until a real field/projection owns it. |
| Organization onboarding | Establish real `organizations` creation/linking and an RLS-safe facility-claim/creation receiver before relying on provider onboarding. |
| Facility claim status | Treat `verification_status = pending` as claimed/reviewing even when `verified = false`; do not use `verified` alone to decide claim availability. |
| Hospital facility management | Add/derive app-discovery taxonomy fields, fix ER-wait update receiver, map Google/discovery responses from `{ data }`, and render `dispatch_eligible`/`booking_eligible` as derived truth rather than inferred UI copy. |
| Driver assignment | Define whether direct profile assignment is allowed while a call is active and how failover appears to operators. |
| Doctor create/invite | Make profile identity the creation owner or link the created row deterministically; avoid create-then-trigger duplicate doctor rows. |
| Doctor edits | Define which columns are projected from profile versus owned by the doctor directory. |
| Staff scheduling | Implement org-authorized `doctor_schedules` CRUD/read/conflict/statistics for doctor shifts, remove generated doctor/ambulance shift rows, keep `doctors` availability distinct, and do not expose ambulance shift CRUD without a persisted receiver. |
| Provider media | Verify Storage policy authority, then either persist only supported image fields or remove upload controls that cannot be attached to a row. Hospital image edits need provenance fields or an explicit "manual provider image" policy. |

## Read-Only Receiver Follow-Up

A client-scoped SELECT-only probe confirmed that `hospitals.verification_status`, `profiles.bvn_verified`, and `profiles.organization_id` are selectable live, while `hospitals.rejection_reason`, `hospitals.verified_at`, `hospitals.rejected_at`, and `profiles.verification_status` are not. The probe executed no mutation, RPC, or Edge Function. It confirms the stale legacy approval receiver finding; it does not prove a runtime attempt of those writes.
