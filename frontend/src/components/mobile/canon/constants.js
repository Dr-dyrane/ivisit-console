// Mobile canon kit - centralized behavioral constants (CANON_COMPONENT_SPECS
// "Tokens to add"). One home; pages import instead of re-declaring.
export const SKELETON_WARMUP_MS = 400;   // forced skeleton on EVERY mobile mount
// draft -> committed filter; the value lives with the lane-neutral hook.
export { SEARCH_DEBOUNCE_MS } from '../../../hooks/useSearchDraft';
export const ROUTE_FEEDBACK_MS = 320;    // opening-feedback window
export const SCROLL_COOLDOWN_MS = 180;   // KPI rail scroll cooldown
export const LOAD_MORE_ROOT_MARGIN = '120px';
export const HAPTIC_THROTTLE_MS = 260;
