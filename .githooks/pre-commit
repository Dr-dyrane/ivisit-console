#!/bin/sh

set -e

if git diff --cached --name-only --diff-filter=ACMR | grep -Eq '(^frontend/src/types/database\.ts$|^frontend/supabase_types\.ts$|^frontend/supabase/|database)'; then
  (cd frontend && npm run check:database-types-encoding)
fi
